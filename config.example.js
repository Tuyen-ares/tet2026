// Copy this file to `config.js` in the same folder and set your backend URL.
// Example for deploying frontend separately from backend:
// window.__API_BASE__ = 'https://api.example.com';

window.__API_BASE__ = '';
